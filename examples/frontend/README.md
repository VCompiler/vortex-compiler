# Frontend Examples

这个目录用于放置前端接入相关样例和中间产物。

当前第一阶段只固定三类内容：

1. `pytorch/`
   PyTorch toy 模型与 ONNX 导出脚本
2. `onnx/`
   导出的 ONNX 模型与元数据
3. `mlir/`
   后续 ONNX-MLIR 中间 IR 快照

当前目标不是把前端完整做完，而是先固定：

1. 输入模型
2. 导出方式
3. 产物位置
4. 后续桥接边界
